#ifndef STOP_H
#define STOP_H

#include <QString>
#include <QVector>
#include <QImage>

class Stop {
public:
    // Constructor. Takes a list of file paths.
    Stop(const QString& name, const QString& description, const QVector<QString>& photoFilePaths);

    // Getters
    QString getName() const;
    QString getDescription() const;
    QVector<QImage> getPhotos() const;

private:
    QString m_name;
    QString m_description;
    QVector<QImage> m_photos;
};

#endif // STOP_H
